from typing import Any

class EmbeddingService:
    @staticmethod
    async def ingest(filename: str, content: bytes) -> int:
        # Placeholder: store document and return ID
        # In real use, store embeddings in a vector DB
        return 1
